const container=document.querySelector(".portfolio__container"),items=Array.from(container.children);function getColumnCount(){return window.innerWidth>=1280?3:window.innerWidth>=768?2:1}const columns=getColumnCount(),reorderedItems=Array.from({length:columns},(()=>[]));items.forEach(((n,e)=>{reorderedItems[e%columns].push(n)})),container.innerHTML="",reorderedItems.flat().forEach((n=>container.appendChild(n)));